
# ── Core ───────────────────────────────────────────────────────────────────
langgraph>=0.2.0
psutil>=5.9.0
python-dotenv>=1.0.0
prometheus-client>=0.20.0

# ── LLM providers ──────────────────────────────────────────────────────────
# OpenSRE is provider-agnostic (set LLM_PROVIDER). Anthropic is the default and
# is installed below; the others are optional — install only what you use.
anthropic>=0.40.0
# openai>=1.0.0          # LLM_PROVIDER=openai (also Azure / Groq / OpenRouter / Ollama via OPENAI_BASE_URL)
# google-genai>=1.0.0    # LLM_PROVIDER=google (Gemini)

# ── Notification channels ─────────────────────────────────────────────────
# Slack (interactive bot with Fix It / Ignore buttons)
slack-bolt>=1.18.0

# Telegram (install this to enable Telegram alerts)
python-telegram-bot>=20.0

# WhatsApp via Twilio (install this to enable WhatsApp alerts)
twilio>=8.0.0

# ── Optional — only needed for real AWS/K8s/DB integration ────────────────
# boto3>=1.34.0            # AWS CloudWatch / EC2
# psycopg2-binary>=2.9.9   # PostgreSQL real mode
# kubernetes>=29.0.0       # Kubernetes Python client
